foreach(day RANGE 1 9)
    add_executable("day${day}" "day${day}.c")
    target_compile_options("day${day}" PUBLIC "-Wall")
    target_link_libraries("day${day}" common)
    target_include_directories("day${day}" PUBLIC "${PROJECT_SOURCE_DIR}/shared")
endforeach()
